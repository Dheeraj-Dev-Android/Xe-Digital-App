<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/policyCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:stateListAnimator="@animator/card_state_list_anim"
    app:cardBackgroundColor="@color/card_background"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:rippleColor="@color/ripple_color"
    app:strokeColor="@color/stroke_color"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Left accent bar — color driven by policy type in adapter -->
        <View
            android:id="@+id/policy_type_indicator"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:layout_marginVertical="14dp"
            android:background="@drawable/bg_accent_bar"
            android:backgroundTint="@color/NationalHoliday"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Main content -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:paddingVertical="14dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/policy_type_indicator"
            app:layout_constraintTop_toTopOf="parent">

            <!-- Icon container with soft gradient background -->
            <FrameLayout
                android:id="@+id/policy_icon_container"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:background="@drawable/bg_empty_state_circle"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <com.google.android.material.imageview.ShapeableImageView
                    android:id="@+id/btn_policyIcon"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_gravity="center"
                    android:padding="12dp"
                    android:scaleType="centerInside"
                    android:src="@drawable/ic_eye"
                    app:tint="@color/accent_employee" />

            </FrameLayout>

            <!-- Policy name — primary text -->
            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/tv_policy_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:textColor="@color/text_primary"
                android:textSize="16sp"
                app:layout_constraintEnd_toStartOf="@id/policy_arrow"
                app:layout_constraintStart_toEndOf="@id/policy_icon_container"
                app:layout_constraintTop_toTopOf="@id/policy_icon_container"
                tools:text="Travel Policy" />

            <!-- Policy date — secondary text -->
            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/tv_policy_date"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:layout_marginTop="6dp"
                android:letterSpacing="0.01"
                android:textColor="@color/text_secondary"
                android:textSize="12sp"
                app:layout_constraintBottom_toBottomOf="@id/policy_icon_container"
                app:layout_constraintEnd_toStartOf="@id/policy_arrow"
                app:layout_constraintStart_toEndOf="@id/policy_icon_container"
                app:layout_constraintTop_toBottomOf="@id/tv_policy_name"
                tools:text="Created on: Jun 21, 2023" />

            <!-- Arrow indicator — signals tappability -->
            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/policy_arrow"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:alpha="0.4"
                android:src="@drawable/ic_chevron_right"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:tint="@color/text_primary" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>